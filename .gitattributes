# Store everything with LF in the repository, regardless of the platform it was
# edited on. Without this, editing on Windows and Linux produces diffs that are
# nothing but line endings.
* text=auto eol=lf

# Binary — never touch these.
*.png binary
